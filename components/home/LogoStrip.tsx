export default function LogoStrip() {
  return (
    <section className="bg-dark-bg border-y border-dark-muted py-7">
      {/* <div className="max-w-[1200px] mx-auto px-6">
        <p className="text-center text-[11px] font-medium tracking-[1.5px] uppercase text-[#444]">
          Trusted by teams building in health
        </p>
        <div className="mt-5 flex flex-wrap items-center justify-center gap-x-10 gap-y-3">
          {PARTNERS.map((name) => (
            <span
              key={name}
              className="text-[12px] font-medium tracking-[0.5px] text-[#555]"
            >
              {name}
            </span>
          ))}
        </div>
      </div> */}
    </section>
  );
}
